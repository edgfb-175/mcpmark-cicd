on:
  push:
    branches: [ main ]

jobs:
  pre-deployment:
    runs-on: ubuntu-latest
    outputs:
      issue-number: ${{ steps.create-issue.outputs.issue-number }}
      previous-sha: ${{ steps.get-previous-sha.outputs.previous-sha }}
      current-sha: ${{ steps.get-current-sha.outputs.current-sha }}
      package-version: ${{ steps.get-package-version.outputs.package-version }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2 # Get previous commit

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Run lint
        run: npm run lint || echo "Lint check completed (allowing warnings for this example)"

      - name: Run tests
        run: npm test

      - name: Get previous commit SHA
        id: get-previous-sha
        run: echo "previous-sha=$(git rev-parse HEAD^)" >> $GITHUB_OUTPUT

      - name: Get current commit SHA
        id: get-current-sha
        run: echo "current-sha=$(git rev-parse HEAD)" >> $GITHUB_OUTPUT

      - name: Get package version
        id: get-package-version
        run: echo "package-version=$(jq -r .version package.json)" >> $GITHUB_OUTPUT

      - name: Create deployment tracking issue
        id: create-issue
        uses: actions/github-script@v7
        with:
          script: |
            const issue = await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `Deployment Tracking - ${{ steps.get-current-sha.outputs.current-sha }}`,
              body: `Deployment in progress for commit ${{ steps.get-current-sha.outputs.current-sha }}`,
              labels: ['deployment', 'in-progress']
            });
            return issue.data.number;
          result-encoding: string

      - name: Post pre-deployment comment
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: ${{ steps.create-issue.outputs.issue-number }},
              body: "Pre-deployment checks completed"
            });

  rollback-preparation:
    needs: pre-deployment
    runs-on: ubuntu-latest
    outputs:
      artifact-name: ${{ steps.create-artifact.outputs.artifact-name }}
      checksum: ${{ steps.generate-checksum.outputs.checksum }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Create rollback directory
        run: mkdir -p rollback-artifacts

      - name: Create rollback script
        run: |
          cat > rollback-artifacts/rollback.sh << 'EOF'
          #!/bin/bash
          set -euo pipefail

          echo "Starting rollback to commit ${{ needs.pre-deployment.outputs.previous-sha }}"

          # Verify git state
          if ! git rev-parse --verify ${{ needs.pre-deployment.outputs.previous-sha }} > /dev/null 2>&1; then
            echo "Error: Previous commit ${{ needs.pre-deployment.outputs.previous-sha }} not found"
            exit 1
          fi

          # Rollback code
          git checkout ${{ needs.pre-deployment.outputs.previous-sha }}

          # Reinstall dependencies
          npm ci

          # Verify installation
          if ! npm list > /dev/null 2>&1; then
            echo "Error: Dependency installation failed during rollback"
            exit 1
          fi

          echo "Rollback completed successfully to commit ${{ needs.pre-deployment.outputs.previous-sha }}"
          EOF
          chmod +x rollback-artifacts/rollback.sh

      - name: Create configuration backups
        run: |
          cp package.json rollback-artifacts/package.json.backup
          cp package-lock.json rollback-artifacts/package-lock.json.backup
          if [ -f .env.example ]; then cp .env.example rollback-artifacts/.env.example.backup; fi

      - name: Create dependency verification script
        run: |
          cat > rollback-artifacts/verify-dependencies.sh << 'EOF'
          #!/bin/bash
          set -euo pipefail

          echo "Verifying dependency compatibility with previous commit"

          # Compare package versions
          diff package.json rollback-artifacts/package.json.backup || echo "Dependency changes detected (expected during rollback)"

          # Verify lockfile integrity
          npm ci --dry-run
          echo "Dependency verification completed"
          EOF
          chmod +x rollback-artifacts/verify-dependencies.sh

      - name: Create rollback documentation
        run: |
          cat > rollback-artifacts/ROLLBACK_GUIDE.md << 'EOF'
          # Rollback Guide for Deployment ${{ needs.pre-deployment.outputs.current-sha }}

          ## Overview
          This guide provides step-by-step instructions to rollback to the previous commit ${{ needs.pre-deployment.outputs.previous-sha }}

          ## Prerequisites
          - Git installed
          - Node.js 20 installed
          - Access to the repository

          ## Rollback Steps
          1. Navigate to the repository directory
          2. Run the rollback script: `./rollback.sh`
          3. Verify the rollback with: `./verify-dependencies.sh`
          4. Restart the application if needed

          ## Emergency Rollback
          If the automated script fails:
          1. Manually checkout the previous commit: `git checkout ${{ needs.pre-deployment.outputs.previous-sha }}`
          2. Reinstall dependencies: `npm ci`
          3. Verify application functionality
          EOF

      - name: Compress rollback package
        id: create-artifact
        run: |
          ARTIFACT_NAME="rollback-package-${{ needs.pre-deployment.outputs.current-sha }}"
          tar -czf $ARTIFACT_NAME.tar.gz rollback-artifacts/
          echo "artifact-name=$ARTIFACT_NAME" >> $GITHUB_OUTPUT

      - name: Generate SHA256 checksum
        id: generate-checksum
        run: |
          CHECKSUM=$(sha256sum ${{ steps.create-artifact.outputs.artifact-name }}.tar.gz | awk '{print $1}')
          echo "checksum=$CHECKSUM" >> $GITHUB_OUTPUT

      - name: Upload rollback artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.create-artifact.outputs.artifact-name }}
          path: ${{ steps.create-artifact.outputs.artifact-name }}.tar.gz
          retention-days: 30

      - name: Post rollback plan comment
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: ${{ needs.pre-deployment.outputs.issue-number }},
              body: `# 🔄 Rollback Plan Ready

              **Previous Commit: ${{ needs.pre-deployment.outputs.previous-sha }}**
              **Current Commit: ${{ needs.pre-deployment.outputs.current-sha }}**
              **Package Version: ${{ needs.pre-deployment.outputs.package-version }}**
              **Artifact: ${{ steps.create-artifact.outputs.artifact-name }}**

              ✅ Rollback script created with error handling
              ✅ Configuration backups (package.json, package-lock.json)
              ✅ Dependency verification script
              ✅ Detailed rollback documentation
              ✅ Compressed rollback package with checksums

              ## Quick Rollback Command
              \`\`\`bash
              # Download and extract the rollback artifact
              wget https://github.com/${{ context.repo.owner }}/${{ context.repo.repo }}/actions/runs/${{ github.run_id }}/artifacts/${{ steps.create-artifact.outputs.artifact-name }}
              tar -xzf ${{ steps.create-artifact.outputs.artifact-name }}.tar.gz
              cd rollback-artifacts
              ./rollback.sh
              \`\`\`

              **Rollback script created: true**
              **Configuration backup: true**
              **SHA256: ${{ steps.generate-checksum.outputs.checksum }}**
              `
            });

  post-deployment:
    needs: [pre-deployment, rollback-preparation]
    runs-on: ubuntu-latest
    steps:
      - name: Update issue labels
        uses: actions/github-script@v7
        with:
          script: |
            // Remove in-progress label
            await github.rest.issues.removeLabel({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: ${{ needs.pre-deployment.outputs.issue-number }},
              name: 'in-progress'
            });

            // Add completed label
            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: ${{ needs.pre-deployment.outputs.issue-number }},
              labels: ['completed']
            });

      - name: Post final deployment comment
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: ${{ needs.pre-deployment.outputs.issue-number }},
              body: `# Deployment Completed Successfully

              Rollback artifact details:
              - Artifact name: ${{ needs.rollback-preparation.outputs.artifact-name }}
              - SHA256 checksum: ${{ needs.rollback-preparation.outputs.checksum }}
              - Retention period: 30 days
              `
            });

      - name: Close deployment issue
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: ${{ needs.pre-deployment.outputs.issue-number }},
              state: 'closed'
            });
